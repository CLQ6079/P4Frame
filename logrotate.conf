# Logrotate configuration for P4Frame
# Place this file in /etc/logrotate.d/p4frame

/home/pi/P4Frame/logs/video_converter/*.log {
    daily
    rotate 7
    compress
    missingok
    notifempty
    create 0644 pi pi
    sharedscripts
    postrotate
        pkill -HUP -f video_converter.py 2>/dev/null || true
        pkill -HUP -f media_frame.py 2>/dev/null || true
        pkill -HUP -f web.py 2>/dev/null || true
    endscript
}